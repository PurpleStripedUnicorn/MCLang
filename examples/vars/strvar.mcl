
const str allPlayers = "@a";

void main() {
    const str name = "PurpleStriped";
    if ("entity " + allPlayers + "[name=" + name + "]")
        /say Hello PurpleStriped!
}

void other() {
    const str name = "Unicorn";
    if ("entity @a" + allPlayers + "[name=" + name + "]")
        /say Hello Unicorn!
}